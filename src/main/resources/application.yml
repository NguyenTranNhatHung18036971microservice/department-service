
server:
  port: 9002

spring:
  application:
    name: DEPARTMENT-SERVICE

  config:
    import: "optional:configserver:"
  cloud:
    config:
      uri: http://localhost:8888

welcome: N T Khoi cloud

eureka:
  client:
    register-with-eureka:
      value: true
    fetch-regítry:
      value: true
    service-url:
      defaultZone: http://localhost:8761/eureka
    instance:
      hostname: localhost

